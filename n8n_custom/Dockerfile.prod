# n8n_custom/Dockerfile.prod (VERSIÓN SIMPLE)

FROM n8nio/n8n:latest

USER root
# Solo instalamos lo que los nodos necesitan para ejecutarse.
RUN apk add --no-cache pandoc poppler-utils

USER node
# No hay ENTRYPOINT. Usamos el 'n8n start' por defecto de la imagen base.